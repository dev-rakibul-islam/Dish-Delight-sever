# Environment Variables Example File
# Copy this file to .env.local and fill in your actual values
# ⚠️ NEVER commit .env or .env.local to GitHub

# ===========================================
# MongoDB Configuration
# ===========================================
# Get these from MongoDB Atlas (mongodb.com/cloud/atlas)
DB_USER=your_mongodb_username
DB_PASSWORD=your_mongodb_password
DB_NAME=dishdelight

# ===========================================
# JWT Secret for Authentication
# ===========================================
# Generate with: openssl rand -base64 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_generated_secret_key_here

# ===========================================
# CORS Configuration
# ===========================================
# Client origin for CORS (set after Netlify deployment)
# Multiple origins: https://app1.netlify.app,https://app2.netlify.app,http://localhost:3000
CLIENT_ORIGIN=http://localhost:3000

# ===========================================
# Optional: Internal API Key
# ===========================================
# Used for internal API requests (OAuth sync endpoint)
INTERNAL_API_KEY=dev-internal-key

# ===========================================
# Optional: Server Port
# ===========================================
# Default: 5000
PORT=5000
